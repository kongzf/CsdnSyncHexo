language: node_js
node_js: stable
# $: Build Lifecycle
install:
  - npm install


#before_script:
 # - npm install -g gulp

script:
  - npm start

after_script:
  - cd ../
  - git clone "https://${GH_TOKEN}@${GH_REF}" blog
  - cp -r CsdnSyncHexo/_posts/. ./blog/source/_posts
  - cd blog
  - git add ./source/_posts/.
  - git commit -m "同步csdn博客"
  - git push

branches:
  only:
    - master

env:
 global:
   # Github Pages
   - GH_REF: github.com/kongzf/blog.git
